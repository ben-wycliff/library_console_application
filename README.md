# library_console_application

Library console applications demonstrates basic CRUD operations on a mongoDB database with python

## Database
Make sure to you have installed mongoDB and run it as a service locally

## Usage
Clone this repository onto your local machine

Install and activate virtual environment

Install requirements
```bash
pip install -r requirement.txt
```
Run application
```bash
python app.py
```
